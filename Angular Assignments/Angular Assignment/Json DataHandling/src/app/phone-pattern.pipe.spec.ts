import { PhonePatternPipe } from './phone-pattern.pipe';

describe('PhonePatternPipe', () => {
  it('create an instance', () => {
    const pipe = new PhonePatternPipe();
    expect(pipe).toBeTruthy();
  });
});
